<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title></title>
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="">
        <link rel="stylesheet" href="/normalize.css" />
        <link rel="stylesheet" href="/skeleton.css" />
        <link rel="stylesheet" href="/index.css">
    </head>
    <body>
        <div class="topnav">
            <% if(currentUser) { %>
            <a href="/">Home</a>
            <a class="active" href="/classes">Classes</a>
            <a href="/classes/scheduler">Scheduler</a>
            <a href="/classes/new">New Class</a>
            <a id="user" href="">Hello <%=user.name %></a>
            
            <form action="/sessions?_method=DELETE" method="POST">
                <input type="submit" value="Log Out" />
            </form>
            <% } else { %>
            <a href="/">Home</a>
            <a class="active" href="/classes">Classes</a>
            <a href="/classes/new">New Class</a>
            <a href="/about">About</a>
            <a id="signin" href="/users/new">Register</a>
            <a id="signin1" href="/sessions/new">Login</a>
            <% } %>
        </div><br>
        <div class="container">
            <h1>Hello Climber</h1>
            <table>
                <% for(let i = 0; i < allClasses.length; i++) { %>  
                <tr>
                    <td>
                        <% if(currentUser) { %>
                        <button onclick="window.location.href='/classes/<%=allClasses[i].id%>'">More info</button>
                        <% }else { %>
                            <button onclick="window.location.href='/sessions/new'">More info</button>
                        <% } %>
                    </td>
                    <td>
                        Class:<br><%=allClasses[i].name;%>
                        <!-- <img src="<%=allClasses[i].img %>"> -->
                    </td>
                    <td>   
                        Class description:<br><%=allClasses[i].description %>
                    </td>
                    <td>
                       Classes on:<br><%=allClasses[i].day %>
                    </td>
                </tr>
                <% } %>
            </table>
        </div>
        <script src="" async defer></script>
    </body>
</html>